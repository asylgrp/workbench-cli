default:
    suites:
        default:
            contexts:
                - FeatureContext:
                    executable: bin/workbench
        phar:
            contexts:
                - FeatureContext:
                    executable: workbench.phar
